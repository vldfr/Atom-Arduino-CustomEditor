'use babel';

import AtomArduinoCustomEditorView from '../lib/atom-arduino-custom-editor-view';

describe('AtomArduinoCustomEditorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
